<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zf1976.ant.upms.biz.dao.SysDepartmentDao">
    <!--查询部门依赖关系-->
    <select id="selectDependsOnById" resultType="java.lang.Long">
        select count(*)
        from sys_role_department_relation
        where department_id = #{id}
    </select>

    <!--删除角色与部门关联-->
    <delete id="deleteRoleRelationByIds">
        delete
        from sys_role_department_relation
        where department_id
        in
        <foreach collection="ids" item="id" index="index" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <!--获取角色所有部门-->
    <select id="selectListByRoleId" resultType="com.zf1976.ant.upms.biz.pojo.po.SysDepartment">
        select id,
               pid,
               name,
               dept_sort,
               enabled,
               create_by,
               update_by,
               create_time,
               update_time
        from sys_department
                 join sys_role_department_relation srd on sys_department.id = srd.department_id
        where role_id = #{roleId};
    </select>

    <!--获取子部门-->
    <select id="selectChildrenById" resultType="com.zf1976.ant.upms.biz.pojo.po.SysDepartment">
        select id,
               pid,
               name,
               dept_sort,
               enabled,
               create_by,
               update_by,
               create_time,
               update_time
        from sys_department
        where pid = #{id}
    </select>

    <!--查询角色依赖关系id-->
    <select id="selectRoleRelationById" resultType="java.lang.Long">
        select role_id
        from sys_role_department_relation
        where department_id = #{id}
    </select>
    <select id="selectRoleRelationByIds" resultType="java.lang.Long">
        select distinct role_id
        from sys_role_department_relation
        where department_id
        in
        <foreach collection="ids" item="id" index="index" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>
</mapper>
