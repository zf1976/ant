<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zf1976.ant.auth.dao.SysPermissionDao">

<!--  获取接口权限值-->
    <select id="selectListByResourceId" resultType="java.lang.String">
        select distinct value
        from sys_permission
                 join sys_resource_permission_relation srpr on sys_permission.id = srpr.permission_id
        where srpr.resource_id = #{resourceId}
    </select>

</mapper>
