server:
  port: 9000
#nacos配置
spring:
  cloud:
    nacos:
      config:
        file-extension: yaml
      server-addr: innas.cn:8848
      discovery:
        register-enabled: true
        enabled: true